# 使用说明 · MCP点检助手

一个 **单文件绿色小工具**（约 8 MB，无需安装任何运行时）：在聊天窗口里让 DeepSeek 大模型调用 MCP 点检工具，对软件工程目录执行点检并生成报告。

- 适用系统：Windows 10 / Windows 11（x64）
- 不需要 .NET、Node、Python、VC++ 运行库
- 界面完全离线运行，不加载任何外部 CDN 资源

**界面分为三栏**：左侧是配置（模型 / 工作区 / MCP 服务器），**中间是对话区**（你的提问和模型的反馈、点检报告），**右侧是「MCP 工具调用」面板**（每次工具调用的参数、返回内容、耗时状态都列在这里），中间对话流里只保留一条轻量提示，读报告时不会被工具细节打断。

---

## 一、使用前你需要准备两样东西

1. **DeepSeek API Key**
   到 DeepSeek 开放平台申请（形如 `sk-xxxxxx`）。工具只调用 DeepSeek 官方接口 `https://api.deepseek.com`。
2. **MCP 点检服务器的地址**（http）
   由点检工具的服务方提供，形如 `http://192.168.1.10:3000/mcp`。
   本工具**只支持 http/streamableHttp/sse 三种 http 类型**；`command`（本地 stdio）类型暂不支持，会在界面标注「暂不支持」。

## 二、快速开始（4 步）

1. **双击 `MCP点检助手.exe`** 打开窗口（首次运行如弹出 SmartScreen 提示，点「更多信息」→「仍要运行」即可，这是未购买代码签名证书的正常现象）。
2. **填写模型设置**：粘贴 API Key → 点「保存配置」。
   - 模型名称默认 `deepseek-chat`；如你的 Key 使用新型号，点「获取」按钮会自动拉取可用模型列表，选择即可。
3. **选择工作区文件夹**：点「选择文件夹」，在弹出的对话框里选中要点检的软件工程目录。之后每次对话，模型都会基于这个目录工作。
4. **加载 MCP 服务器**：在「MCP 服务器」一栏确认 mcp.json 路径后点「加载」。列表里显示 **绿色「已连接」** 并展开能看到工具名，即表示就绪。

然后在下方输入框打字（例如「请对当前工作区执行点检，输出问题清单和修复建议」），按 **Enter** 发送（Shift+Enter 换行），即可看到模型逐步调用 MCP 工具、返回结果、最终生成报告的全过程。

> 「新会话」按钮可清空上下文重新开始。

### 关于右侧「MCP 工具调用」面板

- 每次模型调用 MCP 工具，右侧都会新增一张卡片：**工具名 + 状态**（🟡 调用中 / 🟢 完成 / 🔴 失败）。
- 卡片内分两个浅色块，便于一眼区分：
  - **浅黄色块 = 参数**（这次调用传给工具的内容）
  - **浅绿色块 = 返回**（工具返回的内容及字数；失败时为浅红色「错误」块）
- 返回内容很长时默认折叠，点块内的 **「展开全文」** 可看完整明细。
- **面板宽度可调**：把鼠标移到聊天区和右侧面板之间的分隔条上，按住左键左右拖动即可拉宽/收窄（向左拖变宽，最小 260px，最大随窗口宽度）；**双击分隔条**恢复默认宽度。调整后的宽度会自动记住，下次打开不用重调。
- 面板顶部的 **「清空」** 按钮可只清空调用记录，不影响中间对话；**「新会话」** 会同时清空两者。
- 需要核对某个工具到底返回了什么时，看右侧面板即可，不用在对话记录里翻找。

### 关于大报告

点检结果很长时（例如包含成百上千条明细）：

- 右侧面板里的工具返回**完整保留全部内容**，默认折叠显示前几屏，点 **「展开全文」** 按钮可查看完整明细，标签旁会显示总字数。
- 送给模型的同一条结果会自动做长度保护（防止超出模型上下文导致请求失败），因此最终报告由模型基于其收到的主要内容生成；需要逐条核对明细时，请展开右侧卡片的返回内容查看原文。

## 三、mcp.json 怎么写

工具启动时默认读取 **exe 同目录下的 `mcp.json`**（也可在界面里改成其他路径）。

格式与 Cursor 相同，例如：

```json
{
  "mcpServers": {
    "dianjian": {
      "url": "http://192.168.1.10:3000/mcp",
      "type": "streamableHttp",
      "headers": {
        "Authorization": "Bearer your-token"
      }
    }
  }
}
```

| 字段 | 说明 |
| --- | --- |
| `url` | **必填**，MCP 服务器地址（http）。 |
| `type` | 可省略。`streamableHttp`/`http` 或省略 = Streamable HTTP；`sse` = SSE 传输。 |
| `headers` | 可选，自定义请求头（如鉴权 Token）。 |
| `command` | 暂不支持（本地 stdio 类型），界面会标注「暂不支持」。 |

配置改完后点界面上的「加载」按钮即可生效，无需重启。

## 四、常见问题

**Q：窗口是空白的 / 提示缺少 WebView2？**
少数精简版 Win10 未预装 WebView2 运行时。工具会自动改用你的默认浏览器打开同样的界面，功能完全一致。也可以在微软官网安装「WebView2 Runtime (Evergreen)」后获得原生窗口体验。

**Q：提示「当前没有已连接的 MCP 服务器」？**
检查 mcp.json 里的地址是否可达（可在浏览器直接访问该地址确认服务在线）、端口是否开放、是否需要 headers 鉴权。修好后点「加载」。

**Q：保存配置失败 / 无法写入？**
请把 exe 放在**本地可写目录**（如桌面、D 盘某个文件夹），不要放在只读位置或只读网络共享上。

**Q：API Key 安全吗？**
Key 只保存在你本机的 `config.json`（exe 同目录），仅用于调用 DeepSeek 接口。本地服务只监听 `127.0.0.1`，局域网内其他电脑无法访问。

**Q：想彻底清理？**
直接删除整个文件夹即可（包含 `MCP点检助手.exe`、`config.json`、`mcp.json`、`mcpcheck.log`）。无注册表残留、无安装动作。

## 五、文件说明（exe 同目录）

| 文件 | 作用 | 是否可删 |
| --- | --- | --- |
| `MCP点检助手.exe` | 主程序 | — |
| `config.json` | 你的 API Key / 模型 / 工作区等设置 | 可删（恢复默认） |
| `mcp.json` | MCP 服务器清单 | 可删 |
| `mcpcheck.log` | 运行日志（排障用） | 可删 |
| `MCP点检助手.exe.webview2\` | WebView2 浏览器缓存目录 | 可删 |
